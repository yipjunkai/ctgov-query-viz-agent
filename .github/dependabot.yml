version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (grouped into one PR).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      actions:
        patterns: ["*"]
