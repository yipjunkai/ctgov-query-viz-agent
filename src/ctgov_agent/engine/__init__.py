"""Deterministic engine: IR → API requests → aggregation → visualization spec.

Nothing here calls an LLM. Every number in the output is computed from real records, which is what
lets the citations be verified and the counts be trusted.
"""
